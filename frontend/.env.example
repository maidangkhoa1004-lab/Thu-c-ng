# URL của backend Flask đã deploy (không có dấu / ở cuối)
# Local dev: để trống hoặc xóa dòng dưới, app sẽ tự dùng http://127.0.0.1:5000
# Production (Vercel): điền URL backend thật, vd: https://ten-app.onrender.com
VITE_API_URL=
